# Rust Cargo DOS Commander - Python Dependencies
# tkinter is part of Python standard library (no pip install needed)
# Pillow is optional, included for potential future icon support
Pillow==10.4.0

# ---------- Development & Testing Dependencies ----------
pytest==8.3.3
pytest-cov==5.0.0
pyinstaller==6.10.0
